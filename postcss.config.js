module.exports = {
  plugins: {
    // This is the new Tailwind PostCSS plugin package—not `require('tailwindcss')`
    '@tailwindcss/postcss': {},
    // Autoprefixer stays the same
    'autoprefixer': {},
  },
}
